<% if pixel.active %>
  <span class="rounded-full bg-emerald-50 px-2 py-1 text-xs font-medium text-emerald-700">Active</span>
<% else %>
  <span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-medium text-slate-600">Paused</span>
<% end %>
