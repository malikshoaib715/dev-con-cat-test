<% content_for :title, "Edit #{@pixel.name}" %>

<h1 class="text-xl font-semibold">Edit pixel</h1>
<p class="font-mono text-xs text-slate-500"><%= @pixel.public_id %></p>

<%= render "app/pixels/form", pixel: @pixel, url: app_pixel_path(@pixel), submit_label: "Save changes" %>
